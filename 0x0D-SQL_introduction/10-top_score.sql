-- list all records table second_table of the DB
-- hbtn result should display score and name in order
-- record should be ordered by score(top first)
SELECT score, name FROM second_table ORDER BY score DESC;